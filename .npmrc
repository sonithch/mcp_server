# @clerk/mcp-tools has an unrelated peerOptional dependency on an older
# better-sqlite3 (^8.7.0, for a file-store feature we don't use) that
# conflicts with the ^13.0.0 better-auth actually requires. Safe to ignore
# since nothing imports @clerk/mcp-tools's sqlite store.
legacy-peer-deps=true
